---
import Layout from "../layouts/BlogPost.astro";
import * as aboutMe from "../content/hidden/about-me.mdx";

const { Content } = aboutMe;
const data = aboutMe.frontmatter;
data.pubDate = new Date(data.pubDate);
---

<Layout {...data}>
    <Content />
</Layout>
